"""
Built-in context modules for the Context Window system component.

Each subdirectory is a self-contained module with:
  - manifest.json: Module metadata, capabilities, and defaults
  - handler.py: ContextModule subclass implementing contribute/condense/purge
"""
