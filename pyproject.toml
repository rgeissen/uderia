[project]
name = "uderia"
version = "0.1.0"
requires-python = ">=3.8"

[project.optional-dependencies]
teradata = ["teradatagenai>=1.0", "teradataml>=20.0"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
